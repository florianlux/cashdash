{
  "name": "dropcharge-newsletter-dashboard",
  "version": "1.0.0",
  "description": "Dark minimal newsletter dashboard — static frontend + Netlify Functions + Supabase",
  "private": true,
  "scripts": {
    "dev": "netlify dev"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.3"
  },
  "devDependencies": {
    "netlify-cli": "^17.14.0"
  },
  "engines": {
    "node": ">=18"
  }
}
